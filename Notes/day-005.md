# BeeLabs Day 5 — Reusable Functions and Debugging

## What I Built

I upgraded my Virtual Stylist so it can clean and format user input.

## What I Learned

- A function is reusable code that performs a specific job.
- A parameter receives information when a function is called.
- `return` sends a result back to the rest of the program.
- `.strip()` removes extra spaces.
- `.lower()` converts text to lowercase.
- `.title()` formats a name with capital letters.
- Testing unusual inputs helps uncover bugs.

## Function I Created

```python
def get_answer(question):
    return input(question).strip().lower()
    
